/** Production URL when Host is unavailable (e.g. metadata during build). Must match your deployed domain. */
export const CANONICAL_SITE_URL = "https://www.casaconcierge.co";

/** Public contact email (footer, legal, mailto links). */
export const CONTACT_EMAIL = "casaconciergepdc@outlook.com";
